{
  "name": "mask-bridge",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "rm -rf node_modules/.cache && vite build && echo \"bridge.mask.io\" > out/CNAME  && git add out/ && git commit -m \"Deploy Vite to gh-pages\" && git subtree push --prefix out origin gh-pages"
  },
  "dependencies": {
    "vue": "^3.2.25"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^2.2.0",
    "vite": "^2.8.0",
    "vue-router": "^4.0.12",
    "less": "^4.1.2",
    "less-loader": "^6.0.0"
  }
}
